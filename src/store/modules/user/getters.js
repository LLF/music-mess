// User getters
const getters = {
  getFavoriteList: state => state.favoriteList,
  getPlayedHistory: state => state.playedHistory,
  getSearchHistory: state => state.searchHistory
}

export default getters
